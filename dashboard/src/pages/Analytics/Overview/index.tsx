import React from 'react';
import AnalyticsComingSoon from '../ComingSoon';

export default function OverviewPage() {
  return (
    <AnalyticsComingSoon
      pageName="Analytics Overview"
      description="整体 KPI 总览页面暂未交付，先保留占位以保证路由正常，方便后续对接设计稿和图表组件。"
    />
  );
}

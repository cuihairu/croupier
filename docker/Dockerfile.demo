# Deprecated telemetry demo image.
#
# The former source target (services/demo/main.go) was removed from the
# repository. docker-compose.telemetry.yaml no longer builds this image; keep
# this file temporarily so a later, explicitly approved legacy-file cleanup
# can remove it without making the telemetry validation path depend on it.
FROM scratch

LABEL org.opencontainers.image.description="Deprecated Croupier telemetry demo image; not runnable"

# Docker/dev config for croupier-agent (used by docker/docker-compose.yml)
name: croupier-agent
host: 0.0.0.0
port: 19091

server:
  # Upstream control plane address (docker-compose service name)
  addr: "server:19090"
  insecure: true
  serverName: ""
  insecureSkipVerify: false

agent:
  # ID is optional; when empty, agent will auto-generate one at runtime.
  id: ""
  gameId: ""
  env: ""
  # Must be reachable by server within the docker network.
  localAddr: "agent:19091"
  httpAddr: "agent:19091"
  labels: {}

upstream:
  heartbeatInterval: 30
  retryInterval: 5
  maxRetries: 3
  timeout: 10000

tls:
  enabled: false
  certFile: ""
  keyFile: ""
  caFile: ""
  insecureSkipVerify: false

outboundTLS:
  enabled: false
  certFile: ""
  keyFile: ""
  caFile: ""
  serverName: ""
  insecureSkipVerify: false

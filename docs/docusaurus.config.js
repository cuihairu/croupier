// Docusaurus config for Croupier docs living directly under this folder.
// Keep it minimal (KISS) and rely on autogenerated sidebar (DRY).
// We mount docs at '/' and use the current folder as docs source,
// so no content move is required (YAGNI).
/** @type {import('@docusaurus/types').Config} */
const config = {
  title: 'Croupier',
  tagline: '分布式游戏管理系统文档',
  // Update these two if your GitHub Pages root changes
  url: 'https://cuihairu.github.io',
  baseUrl: '/croupier/',
  onBrokenLinks: 'throw',
  onBrokenMarkdownLinks: 'warn',
  favicon: undefined, // No favicon yet
  i18n: {
    defaultLocale: 'zh-Hans',
    locales: ['zh-Hans'],
  },
  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          path: '.', // use current folder as docs source
          routeBasePath: '/', // serve docs at site root
          sidebarPath: require.resolve('./sidebars.js'),
          // Ignore bundled dependencies inside docs/ to avoid MDX parsing 3rd-party READMEs
          exclude: ['**/node_modules/**'],
          editUrl: 'https://github.com/cuihairu/croupier/edit/main/docs/',
          showLastUpdateAuthor: true,
          showLastUpdateTime: true,
        },
        blog: false,
        theme: {
          // no custom CSS for now
        },
      }),
    ],
  ],
  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      navbar: {
        title: 'Croupier',
        items: [
          {
            href: 'https://github.com/cuihairu/croupier',
            label: 'GitHub',
            position: 'right',
          },
        ],
      },
      footer: {
        style: 'dark',
        copyright: `Copyright © ${new Date().getFullYear()} Croupier`,
      },
    }),
};

module.exports = config;

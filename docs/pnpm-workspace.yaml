allowBuilds:
  esbuild: true

# 2026-08-02 安全补丁：修复 Dependabot 报告的可修复漏洞
overrides:
  vite@<=5.4.20: ">=5.4.21 <6.0.0"
  esbuild@<=0.24.2: "~0.25.0"
  postcss@<=8.5.17: ">=8.5.18"
  dompurify@<=3.4.11: ">=3.4.12"
  immutable@>=5.0.0-beta.1 <5.1.8: ">=5.1.8"
  # 保留不修复（2026-08-15 评估）：
  # - vite <=6.4.2（#50/#151/#152）：补丁仅在 6.4.3+，vitepress 1.6.4 依赖 vite ^5，
  #   强制 6.x 构建失败（ERR_MODULE_NOT_FOUND）；需 vitepress 2.x 升级后一并处理。
  #   仅文档站构建期依赖，不随产品发布。

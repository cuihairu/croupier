coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 80%
        threshold: 1%

comment: false

ignore:
  - "proto/**"
  - "pkg/pb/**"
  - "gen/**"
  - "dashboard/**"

github_checks:
  annotations: true

# 本地编译配置 - 不上传 proto 文件到 buf 服务
version: v2
plugins:
  # Go plugins (本地)
  - local: ["protoc-gen-go"]
    out: pkg/pb
    opt:
      - paths=source_relative
  - local: ["protoc-gen-go-grpc"]
    out: pkg/pb
    opt:
      - paths=source_relative
      - require_unimplemented_servers=false

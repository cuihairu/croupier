# yaml-language-server: $schema=https://buf.build/docs/configuration/v2/buf-yaml
version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
    # RPC_* 规则保留启用（无 except）：本仓库 proto 只定义 message，
    # 禁止 service/rpc 定义，杜绝 gRPC 代码生成的前提（docs/grpc-investigation.md）。
breaking:
  use:
    - FILE
